        //Run a function every 1000 ms
        window.setInterval(function(){
            functionX();

        }, 1000);

        //Ajax requests

        function methodY(){
            //Ajax request
            $.get( urlPath, function( data ) {

                dataObj = JSON.parse(data);
                console.log("Latest state fetched from server")
                console.log(dataObj);

        


            });
        }

        //Web UI update

        function methodZ(currentState) {
            $("#elementID").html(currentState);
        }
